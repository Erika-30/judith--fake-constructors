// Constructor function
function FakeArray() {
  //completar
}

// Métodos compartidos

FakeArray.prototype.push = function () {
  // completar
};

FakeArray.prototype.pop = function () {
  // completar
};

FakeArray.prototype.shift = function () {
  // completar
};

FakeArray.prototype.unshift = function () {
  // completar
};

FakeArray.prototype.splice = function () {
  // completar
};

FakeArray.prototype.slice = function () {
  // completar
};

FakeArray.prototype.indexOf = function () {
  // completar
};

FakeArray.prototype.includes = function () {
  // completar
};

FakeArray.prototype.join = function () {
  // completar
};

FakeArray.prototype.reverse = function () {
  // completar
};

FakeArray.prototype.forEach = function () {
  // completar
};

FakeArray.prototype.map = function () {
  // completar
};

FakeArray.prototype.find = function () {
  // completar
};

FakeArray.prototype.filter = function () {
  // completar
};

FakeArray.prototype.sort = function () {
  // completar
};

FakeArray.prototype.reduce = function () {
  // completar
};
